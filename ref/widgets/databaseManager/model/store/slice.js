import { createSlice } from '@reduxjs/toolkit';

const initialState = {
};

export const databaseManagerSlice = createSlice({
  name: 'databaseManager',
  initialState,
  reducers: {

  },
});

export const {

} = databaseManagerSlice.actions;

export default databaseManagerSlice.reducer;
