import { createSlice } from '@reduxjs/toolkit';


const initialState = {
};

export const screenWireframeCanvasSlice = createSlice({
  name: 'screenWireframeCanvas',
  initialState,
  reducers: {

  },
});

export const {

} = screenWireframeCanvasSlice.actions;

export default screenWireframeCanvasSlice.reducer;
