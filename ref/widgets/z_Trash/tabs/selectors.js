// import { createSelector } from '@reduxjs/toolkit';
// import { TABS } from '../../constants/tabs';

// export const selectSelectedTab = (state) => state.explorerSidebar.selectedTab;

// export const selectorExplorerSidebar = createSelector([selectSelectedTab], (selectedTab) => {
//     return {
//         selectedTab: selectedTab,
//         tabs: Object.values(TABS),
//     };
// }
// );
