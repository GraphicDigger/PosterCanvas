import { muiComponents } from '../../muiComponents';
import { uiLibraries } from '../../uiLibraries.js';


export const useUILibraryComponents = () => {
  return {
    uiLibraries,
    muiComponents,
  };
};
