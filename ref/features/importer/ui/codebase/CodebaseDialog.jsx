import { Stack } from '../../../../shared/uiKit/Stack';

export const CodebaseDialog = () => {
  return (
    <Stack direction='column' gap={4}>
            Выберите проект для импорта кода
    </Stack>
  );
};
