import { createSlice } from '@reduxjs/toolkit';

const initialState = {
};

export const projectDatabaseSettingsSlice = createSlice({
  name: 'projectDatabaseSettings',
  initialState,
  reducers: {
  },
});

export const {


} = projectDatabaseSettingsSlice.actions;

export default projectDatabaseSettingsSlice.reducer;
