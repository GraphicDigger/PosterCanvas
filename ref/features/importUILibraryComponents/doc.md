
Рендеринг компонентов на основе кода — Двусторонняя синхронизация

Унифицированный подход к компонентам: единая система представления для импортированных и кастомных компонентов.
Импортированный или Кастомный компонент → Нормализованная модель → Генерация кода → Рендеринг

Ресурсы:
    База компонентов их кодов и пропсов (достаем через хуки в папка model)
        src/entities/component/api/components.data.js - добавляем метаданные о библиотеке и импортах компонента
        src/entities/prop - пропсы компонентов
        src/entities/propValue - значения пропсов
        src/entities/instance - экземпляры компонентов

Требования:
— Клиент может импортировать любую ui библиотеку и использовать готовые ui элементы через нашу систему в своем проекте. На старте нам нужно провернуть это с mui (https://mui.com/material-ui/getting-started/).
— Универсальность фичи: одинаковый принцип работы с импортирвоанными из библиотек и кастомными компонентами(созданные в системе

Реализация:

Цель 1. ✅ Извлечение jsx (через импорт) и props (с помощью плагина) 
    Review: [https://youtu.be/NGNYQUl_DvA]
    Импорт компонента из библиотеки [features/importUILibraryComponents]
        — [componentFactory] создает полные данные компонента
            [dynamicComponentAnalyzer] — динамический анализатор компонента.
            Плагин [vite-plugin-react-docgen-typescript] извлекает пропсы из интерфейса компонента на этапе компиляции и сохраняет их в __docgenInfo.
            В списке компонентов указываем путь, затем через импорт в [getComponentData] получаем исходный код (по его пути) и метаданные пропсов.
        — [componentFactory] нормализует данные: component, prop, propValue, code
        — сохраняем в store


Цель 2. 🏓 Отрисовка компонента в канвасе компонента
        — Изолированная среда выполнения: iframe или Web Workers
        — Извлеч из store и компилировать в React код компонента для рендера (Babel для транспиляции JSX и ES6+)
        — Предоставить в области видимости компонента зависимости через внедрение импортов (react-live или react-eval)



Цель 3. 👋 Единая система рендеринга. Рендеринг на основе кода - отображение компонента по сгенерированному коду

        Создать обертку для каждог окомпонент которая будет перехватывать клики
        Создать AST (абстрактное синтаксическое дерево) компонента:
            Распарсить код компонента, чтобы построить дерево его структуры
            При клике сопоставлять DOM-элементы с узлами в дереве компонентов
        Связат ькаждый элемент с исходным кодом
   
        Получаем код компонента из состояния
        Создаем React-элемент на основе кода
        Рендерим компонент с помощью ComponentRenderer (визуальный рендер)
            A[ComponentRenderer] --> B[DynamicElement]
                                        B --> C[Кастомный компонент]
                                        B --> D[MUI компонент]

        src/features/componentRenderer — визуальны рендер компонента и его дочерних элементов/инстансов
            Сейчас кастомный компонент рендерится с помощью этих компонентов, будет ли импортирвоанный компонент рендерится подобным образом? Необходимо получить решение (улучшить текущие или предложи новое) которое станет универсалным для обоих случаев: импорт компонента из сторонней бибилиотеки или созадние кастомного компонента)
            src/features/componentRenderer/ui/ComponentRenderer.jsx
            src/features/componentRenderer/ui/DynamicElement.jsx

        
Цель 4 👋 Интерактивность элементов - возможность выбора элементов внутри компонента
    
Цель 2. Синхронизация кода компонента и его визуального представления
    Рендер компонента на основе кода из базы данных.
        — Хук для генерации JSX, CSS компонента. [useCodeGeneration.js](mdc:src/features/generateComponentCode/model/hooks/useCodeGeneration.js)
        — [ComponentRenderer.jsx](mdc:src/entities/component/ui/ComponentRenderer.jsx) Родительский компонент который итерируется по массиву элементов/компонентов и использует [DynamicElement.jsx](mdc:src/entities/element/ui/DynamicElement.jsx) для отрисовки


    Задача 2. Связь свойств елементов компонента с пропсами
        [props.data.js](mdc:src/entities/prop/api/props.data.js) [propValue.data.js](mdc:src/entities/propValue/api/propValue.data.js) модель пропсов компонентов. 
        [useProps.js](mdc:src/entities/prop/model/hooks/useProps.js) [usePropValues.js](mdc:src/entities/propValue/model/hooks/usePropValues.js) хуки для работы с пропсами компонента (другие хуки, slice, selectors рядом)

        Список пропсов компонента [PropControl.jsx](mdc:src/features/editorControls/propControl/ui/PropControl.jsx)


        


Цель ... Интерактивный редактор пропсов для настройки компонентов
    Обработка событий (клики, наведения,.. ) в компоненте [Element.jsx](mdc:src/entities/element/ui/Element.jsx)
    ? пропсы нужно прописывать вручную или иожно вытащить из компонента?
